// FIXME: Add some real tests. This is here just to make unit tests pass without using --passWithNoTests.
describe("Netvisor authentication", () => {
  it("Doesn't really do anything", () => {
    expect(1).toBe(1);
  });
});
