query GetMySettings {
  getMySettings {
    employeeNumber
    productPreset
    activityPreset
    projectsPreset
    jiraNotificationIgnore
    showWeekend
    setRemainingHours
    showJiraIssueStatus
    entryTemplates {
      key
      templateName
      duration
      description
      product
      activity
      issue
      client
    }
  }
}

mutation UpdateSettings($settings: UpdateSettingsDto!) {
  updateSettings(settings: $settings) {
    employeeNumber
  }
}

mutation AddEntryTemplate($template: EntryTemplateInput!) {
  addEntryTemplate(template: $template) {
    employeeNumber
    entryTemplates {
      key
      templateName
      duration
      description
      product
      activity
      issue
      client
    }
  }
}

mutation RemoveEntryTemplate($templateKey: RemoveEntryTemplateInput!) {
  removeEntryTemplate(templateKey: $templateKey) {
    employeeNumber
    entryTemplates {
      key
      templateName
      duration
      description
      product
      activity
      issue
      client
    }
  }
}
