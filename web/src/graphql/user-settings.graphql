query GetMySettings {
  getMySettings {
    employeeNumber
    productPreset
    activityPreset
    projectsPreset
  }
}

mutation UpdateSettings($settings: UpdateSettingsDto!) {
  updateSettings(settings: $settings) {
    employeeNumber
  }
}
