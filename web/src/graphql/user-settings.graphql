query GetMySettings {
  getMySettings {
    employeeNumber
    productPreset
    activityPreset
    showWeekend
    setRemainingHours
  }
}

mutation UpdateSettings($settings: UpdateSettingsDto!) {
  updateSettings(settings: $settings) {
    employeeNumber
  }
}
